/// Represents an order
class OrderModel {
  /// The title of the order.
  final String title;

  /// The progress status of the order.
  final String progress;

  const OrderModel({required this.title, required this.progress});
}
