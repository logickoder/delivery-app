/// Represents a user in the system.
class UserModel {
  /// The unique identifier for the user.
  final String id;

  /// Creates a new [UserModel] instance with the specified [id].
  const UserModel({required this.id});
}
